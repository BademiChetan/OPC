#include<stdio.h>
#include<math.h>
#include<stdlib.h>
void main()
{ int t;
scanf("%d",t);
printf("\n");
if (1<=t<=1000)
{
 for (int i=0;i<t;i++)
 { int n;
   double k;

   scanf("%d" "%lf",n, k);
  
   if(n>=1&&k<=(pow(10,9));
  { n=pow(2,n);
   printf("%d", (int)n%(int)k);
  }
  else
  {exit(0);
   }
 }
 }
  else
   exit(0);

   }